# セキュリティポリシー

*English (canonical): [SECURITY.md](SECURITY.md)*

## 対象バージョン

Kappan は 1.0 前です。修正の対象は最新リリースだけです。

| バージョン | 対象 |
|---|---|
| 0.1.x | ✅ |
| 0.1 未満 | ❌ |

## 脆弱性の報告

**セキュリティに関する問題を公開 issue に書かないでください。**

GitHub の非公開脆弱性報告を使ってください。
[脆弱性を報告する](https://github.com/SilentMalachite/Kappan/security/advisories/new)

次を含めてください。

- バージョン（`kappan --version`）と実行環境
- 最小の再現手順。入力サイト、実行したコマンド、観測した挙動
- 攻撃者が何を得るのか。できるだけ具体的に

1 週間以内に一次回答をします。受理した場合は公開時期をご相談のうえ、希望がなければ advisory に報告者として記載します。

## 脅威モデル

Kappan はローカルのファイルを読み、ローカルのファイルを書くコマンドラインツールです。対象の内外をはっきりさせておくと、報告する手間が減ります。

### 対象に含まれるもの

- `--out` の外への書き込み。permalink・slug・`static/` の要素を経由したパストラバーサル
- `--out` の外を消すこと、および kappan が作ったものでない `--out` を消すこと（[ADR-0007](docs/ja/adr/0007-out-dir-deletion-policy.md)）
- `kappan serve` が生成サイト外のファイルを配信すること、`--host` の明示なしに loopback を越えて bind すること
- 壊れてはいるがあり得る入力での、クラッシュ・ハング・メモリの無制限な増加
- リリースアーカイブの中身が、そのタグのソースをビルドした結果と一致しないこと

### 対象に含まれないもの

- **Markdown 中の生 HTML とスクリプト。** Kappan は cmark-gfm の `tagfilter` 拡張を有効にしており危険なタグは無効化されますが、それ以外の生 HTML は設計として素通りします（[ADR-0003](docs/ja/adr/0003-gfm-extensions.md)）。`content/` の中身はテンプレートと同じく信頼できる入力です。信頼できない Markdown からサイトを作る場合は、ご自身でサニタイズしてください。
- **`kappan serve` をネットワークに公開すること。** 既定は `127.0.0.1` で、開発用のプレビューサーバーです。公開配信向けの堅牢化はしていません。生成物の前に通常の Web サーバーを置いてください。
- 依存ライブラリの脆弱性。ただし kappan 側の使い方が原因で到達可能になっている場合は対象です。上流へ報告したうえで、こちらにも知らせてください。固定しているバージョンを上げます。
- すでにソースツリーへ書き込める、またはあなたの権限でコードを実行できる攻撃者を前提とするもの。

## リリースの検証

すべてのリリースに、4 アーカイブぶんの `SHA256SUMS` が付いています。

```bash
sha256sum -c SHA256SUMS --ignore-missing   # macOS は shasum -a 256 -c
```

バイナリにはコード署名も公証もしていません。理由は [ADR-0011](docs/ja/adr/0011-release-distribution.md) に記録しています。バイナリはすべて [`.github/workflows/release.yml`](.github/workflows/release.yml) が GitHub の runner 上でタグのコミットからビルドしたもので、メンテナの手元から上げたものは含みません。
