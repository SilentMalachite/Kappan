# CLAUDE.md — Kappan（活版）

> **まず `AGENTS.md` を読むこと。** 実装規約・アーキテクチャ・禁止事項はすべてそちらにある。
> 本ファイルは Claude Code 固有の運用のみを追加する。AGENTS.md の内容を上書きしない。

---

## 1. このプロジェクトでの Claude の役割

このリポジトリでは役割を分担している。

| 担当 | 主な仕事 |
|---|---|
| **Claude** | 設計、仕様策定、ADR、インターフェース定義、レビュー、詰まった箇所の調査 |
| **Codex** | 仕様が確定した部分の実装、機械的な修正 |

したがって Claude は、**書き始める前に「何を作るか」を確定させることに時間を使う**。
大きな実装を一気に書き上げるより、`docs/spec/` と `include/kappan/` のヘッダを先に固めるほうが価値が高い。

ただし、以下は Claude が直接実装してよい:
- 設計判断を伴う中核ロジック（Site モデル構築、テンプレート変数の受け渡し）
- 仕様が言葉で書きにくく、コードで示したほうが早いもの
- テストコード

---

## 2. セッションの進め方

### 開始時
1. `AGENTS.md` と、直近の `docs/adr/` を確認する
2. 現在どのフェーズにいるかを 1 行で確認する（AGENTS.md §10）
3. 今回のセッションのゴールを 1 つに絞る

### 1 セッション 1 テーマ
複数のフェーズや無関係な話題を同じセッションで混ぜない。
話題が変わったら、そこで区切って新しいセッションを提案すること。

### 提案 → 承認 → 実行
コードを書く前に必ずこの形式で提示する。

```
【変更するファイル】
- src/site/collection.cpp（新規）
- include/kappan/site.hpp（Collection の宣言を追加）

【方針】
Document の配列を種別ごとにまとめる Collection を追加する。
ソート順は front matter の date 降順、date なしは末尾。

【確認したいこと】
draft: true の記事を Collection に含めるか、除外するか
```

承認なしに実装を始めない。

---

## 3. 対話の作法

- **1 度に聞く質問は 1 つまで。** 複数あるなら、最も重要な 1 つを選ぶ
- 確認事項は、選択肢を挙げて「A か B か」の形にする。自由記述を求めない
- 長い説明より、順序付きの手順とチェックリストを優先する
- コードを提示するときは、**変更箇所だけ**を示す。ファイル全体を再掲しない
- 前提を積み上げる説明を避ける。結論を先に書き、理由を後に置く

---

## 4. コード提示のルール

- 1 回の応答で提示する新規コードは **200 行以内**。超えるなら分割して段階的に進める
- コードを出したら、必ず「次に実行するコマンド」を添える
  ```
  cmake --build --preset dev && ctest --preset dev --output-on-failure
  ```
- ビルドが通ることを確認できていない場合は、**そう明記する**。「動くはずです」と書かない
- 既存コードを読まずに変更を提案しない。該当ファイルを先に読む

---

## 5. 詰まったときの手順

1. **エラーメッセージ全文**を確認する（一部だけで推測しない）
2. 最小再現ケースを `tests/fixtures/` に作る
3. それでも解決しないなら、原因の仮説を 2〜3 個挙げ、**それぞれの切り分け方法**を示す
4. 2 回試して直らない実装方針は、粘らずに設計自体を見直す提案をする

「よくある原因」を並べるだけの回答をしない。このコードベースの実際の状態を確認してから答える。

---

## 6. 調査系タスク

- ライブラリの使い方は、記憶ではなく実際のヘッダ（`vcpkg_installed/` 配下）を読んで確認する
- 特に `cmark-gfm`（C API）と `inja` は、バージョンによる差が大きい。**必ず現物を見る**
- Web 検索した情報は、この環境で動くか未確認である旨を添える

---

## 7. レビュー時の観点

Codex や自分が書いたコードをレビューするとき、以下の順で見る。

1. **エラー処理** — `tl::expected` を返すべき場所で例外を投げていないか
2. **所有権** — 生ポインタ、ダングリング参照、不要なコピー
3. **UTF-8 / CJK** — バイト単位の処理で日本語を壊していないか
4. **パス** — 文字列連結でパスを作っていないか、Windows の区切り文字
5. **AGENTS.md 準拠** — 命名、警告、依存の無断追加

指摘は「問題の箇所 → なぜ問題か → 修正案」の 3 点セットで書く。
問題がなければ、無理に指摘を作らずに「問題なし」と書く。

---

## 8. モデル使い分けの目安

| 作業 | 目安 |
|---|---|
| 実装、テスト追加、既知パターンの修正 | Sonnet |
| アーキテクチャ設計、テンプレート変数モデルの設計、原因不明のバグ調査 | Opus |

---

## 9. やらないこと

- 承認前にファイルを書き換える
- 指示していない箇所を「ついでに」直す
- 「素晴らしい質問です」等の前置き
- できていないことを、できたかのように書く
- 迷ったまま推測で進める（迷ったら止めて聞く）
